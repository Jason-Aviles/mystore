# Implementation Plans

Generated by the improve skill on 2026-07-10, focused per the owner's request:
finish the homegrown Klaviyo (flows/automation), make the admin feel complete,
and rebuild the hero video moment. This repo is NOT a git repository — plans
carry no commit SHA; every plan's drift check is "compare Current-state
excerpts before starting."

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Automated email-flow engine (welcome, thank-you, abandoned, back-in-stock) | P1 | L | — | DONE |
| 002 | Admin Flows page + HTML campaigns w/ preview & test-send | P1 | M | 001 | DONE |
| 003 | Hero "Monolith" — 3D video slab, fixes scroll-drift bug | P2 | M | — | DONE |
| 004 | Fulfillment workflow (tracking + shipped email) + dashboard pulse | P2 | M | 001 (step 3 only) | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (rationale)

## Dependency notes

- 002 reads the `flow_sends` table and `site_settings.data.flows` toggle
  contract that 001 creates.
- 004 step 3 (shipped email) extends 001's `send-flow` function; steps 1–2
  and 4 are independent and may proceed if 001 is delayed.
- 003 is fully independent (frontend hero only) and can run in parallel.
- All live-send behavior across 001/002/004 can only be *deployed-verified*
  once Supabase + Resend are provisioned (see `.env.example`); code-level
  done criteria are still fully checkable now.

## Findings considered and rejected

- **SMS sending via Twilio**: capture + consent exist; sending deferred —
  compliance (TCPA/10DLC registration) unresolved and keys blank. Revisit
  post-launch.
- **Real Klaviyo integration**: the homegrown stack (Supabase + Resend +
  these plans) covers the needed flows at zero subscription cost; adding
  Klaviyo now would duplicate it. Reconsider only if flows outgrow Resend.
- **Per-carrier tracking APIs**: a pasted tracking number + universal
  17track link satisfies the policy promise at this scale.
- **Chart library for the dashboard**: inline SVG sparkline suffices; a
  dependency for one chart is not warranted.

## Execution notes (2026-07-10)

- 004 deviation: orders already had a `tracking` column + input in Orders.jsx (plan assumed `tracking_number`); matched the existing name instead.
- send-flow uses a shared HTML shell builder rather than five verbatim inlined template files — identical rendered output, one place to edit.
- Live-send behavior across 001/002/004 verifies at Supabase/Resend deploy time; all code-level done criteria pass now.
